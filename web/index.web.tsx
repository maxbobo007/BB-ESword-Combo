import { AppRegistry } from 'react-native';
import App from '../App';

AppRegistry.registerComponent('BBESword', () => App);
AppRegistry.runApplication('BBESword', {
  rootTag: document.getElementById('root'),
});
